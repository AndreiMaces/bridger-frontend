import { FormatTextFromCamelCasePipe } from './format-text-from-camel-case.pipe';

describe('FormatTextFromCamelCasePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTextFromCamelCasePipe();
    expect(pipe).toBeTruthy();
  });
});
